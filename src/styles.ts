import styled from 'styled-components'

export const ContentFloatMenu = styled.div`
  background-color: red;
  max-width: calc(100vw - ((100vw - 1180px) / 2));
  margin: 0 auto;
  width: 100%;
  height: 100%;
  /* position: absolute; */

  top: 0;

  z-index: 1000px;
`
