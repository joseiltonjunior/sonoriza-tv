import styled from 'styled-components'

export const Container = styled.div`
  width: 100%;

  display: flex;
  flex-direction: column;

  label {
    margin-bottom: 4px;
    font-size: 14px;
    font-weight: 500;
  }

  select {
    width: 100%;
    padding: 6px 12px;
    border-radius: 4px;
    border: none;
  }
`
